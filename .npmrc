# @cloudflare/next-on-pages@1.13.16 déclare un peer next>=14.3.0 ; on est en
# 14.2.15 (compatible en pratique). Autorise l'install malgré ce conflit —
# nécessaire pour `npm ci` sur la CI Cloudflare Pages.
legacy-peer-deps=true
engine-strict=false
